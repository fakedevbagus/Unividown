# Phase 3D local validation — remaining processing tools UI

Run from the repository root:

```bash
bash scripts/validate_phase3d_local.sh
```

The runner reuses the complete Phase 3C processing gate and verifies status/result integration plus the correct endpoint for video trim, image optimization, subtitle extraction, GIF creation, and optional transcription.

Expected final line:

```text
PASS: Phase 3D remaining processing tools UI validation completed
```
